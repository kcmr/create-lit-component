{
  "name": "{{pkgName}}",
  "version": "0.0.0",
  "description": "{{description}}",
  "module": "src/index.js",
  "main": "{{component}}.js",
  "files": [
    "{{component}}.js",
    "src/"
  ],
  "scripts": {
    "test": "karma start --coverage",
    "test:watch": "karma start --auto-watch=true --single-run=false --coverage",
    "start": "es-dev-server --watch --node-resolve --compatibility esm --open demo/"
  },
  "dependencies": {
    "lit-element": "^2.0.0",
    "lit-html": "^1.0.0"
  },
  "devDependencies": {
    "@open-wc/testing": "2.0.0",
    "@open-wc/testing-karma": "3.0.0",
    "es-dev-server": "1.23.1",
    "webpack-merge": "4.1.5"
  }
}
